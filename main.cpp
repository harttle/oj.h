#include "oj.h"
using namespace oj;

int main() {
    //freopen("test.in", "r", stdin);
    //freopen("test.out", "w", stdout);

    vector<int> v;
    cin >> v;
    cout << v << endl;

    return 0;
}

